-
  I print the Survey Browse Response Report through the wizard.
-
  !python {model: clv_survey}: |
    ctx = {'model': 'clv_survey','active_ids': [(6,0,[ref('clv_survey_partner_feedback')])]}
    data_dict = {'response_ids' : [(6,0,[ref('clv_survey.clv_survey_partner_feedback')])], 'page_number' : True, 'without_pagebreak': True}
    from tools import test_reports
    test_reports.try_report_action(cr, uid, 'action_view_clv_survey_print_answer',wiz_data=data_dict, context=ctx, our_module='clv_survey')

-
  I print the Survey Analysis Report through the wizard.
-
  !python {model: clv_survey}: |
    ctx = {'model': 'clv_survey','active_ids': [(6,0,[ref('clv_survey_partner_feedback')])]}
    data_dict = {'survey_ids' : [(6,0,[ref('clv_survey.clv_survey_partner_feedback')])]}
    from tools import test_reports
    test_reports.try_report_action(cr, uid, 'action_view_clv_survey_print_statistics',wiz_data=data_dict, context=ctx, our_module='clv_survey')

-
  I print the Survey Form Report through the wizard.
-
  !python {model: clv_survey}: |
    ctx = {'model': 'clv_survey','active_ids': [(6,0,[ref('clv_survey_partner_feedback')])]}
    data_dict = {'survey_ids' : [(6,0,[ref('clv_survey.clv_survey_partner_feedback')])], 'page_number' : True, 'without_pagebreak': True}
    from tools import test_reports
    test_reports.try_report_action(cr, uid, 'action_view_clv_survey_print',wiz_data=data_dict, context=ctx, our_module='clv_survey')
