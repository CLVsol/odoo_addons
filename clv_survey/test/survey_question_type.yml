-
  I check the question type of the clv_survey "Initial Partner Feedback".
-
  !python {model: clv_survey.question}: |
    sur_question = self.on_change_type(cr, uid, [ref("clv_survey_Initial_partner_feedback")], 'multiple_textboxes_diff_type')
    assert sur_question
