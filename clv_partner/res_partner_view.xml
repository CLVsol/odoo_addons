<?xml version="1.0" encoding="UTF-8"?>
<openerp>
    <data>

        <record id="view_res_partner_tree" model="ir.ui.view">
            <field name="name">res.partner.tree</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_tree" />
            <field name="arch" type="xml">
                <field name="name" position="after">
                    <field name="clv_partner_code" />
                </field>
            </field>
        </record>

        <record id="view_res_partner_form" model="ir.ui.view">
            <field name="name">res.partner.form</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_form"/>
            <field name="priority" eval="8" />
            <field name="arch" type="xml">
                <notebook position="inside">
                    <page string="CLV" groups="clv_base.group_base_manager">
                        <group>
                            <field name="clv_partner_code" placeholder="CLV Partner Code..."/>
                            <field name="is_clv_partner"/>
                        </group>
                    </page>
                </notebook>
            </field>
        </record>

        <record id="view_res_partner_filter" model="ir.ui.view">
            <field name="name">res.partner.filter</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_res_partner_filter" />
            <field name="arch" type="xml">
                <filter name="supplier" position="after">
                    <separator/>
                    <filter string="Inactive Partners" name="active" domain="[('active','=',0)]" help="Inactive Partners"/>
                    <filter string="CLV Partners" name="clv_partner" domain="[('is_clv_partner','=',1)]" help="CLV Partners"/>
                </filter>
            </field>
        </record>

    </data>
</openerp>
